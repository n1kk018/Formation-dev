import {PetitChien} from './petit-chien';

let variable:string = 'test';
let tableau:number[] = [];
tableau.push(1);

let chien:PetitChien = {
    id: 1,
    nom: "rex",
    race: "corgi",
    dateNaissance: new Date()
}
function test(argument:string, argument2:number):number {
    return 10;
}